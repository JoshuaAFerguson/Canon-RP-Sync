# iPad companion app (planned)

Not started. Intended stack: Swift/SwiftUI, joining the camera hotspot via
`NEHotspotConfiguration`, with the sync core bound from Go via `gomobile`
or reimplemented natively if binding proves awkward.
